@model IEnumerable<Wellness360.Models.KategoriaEUshqimeve>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kategoria)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Kategoria)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Kategoria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Kategoria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Kategoria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
