@model IEnumerable<Wellness360.Models.Vrapimi>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Distanca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Koha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShpejtesiaMesatare)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kalorite)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Distanca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Koha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShpejtesiaMesatare)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kalorite)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Numri">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Numri">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Numri">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
